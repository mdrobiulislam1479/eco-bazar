import CartClient from "@/components/cart/CartClient";

export const metadata = {
  title: "Cart list",
};

const Cart = () => {
  return (
    <div>
      <CartClient />
    </div>
  );
};

export default Cart;
